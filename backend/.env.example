# =============================================================================
# Google ADK Configuration (Agent Development Kit)
# =============================================================================
# Google API key for Gemini models (get at https://aistudio.google.com/apikey)
# Required for SOAP Agent with Google ADK
GOOGLE_API_KEY=your_api_key_here

# =============================================================================
# Ollama Configuration (Local LLM)
# =============================================================================
# Ollama server URL (default: http://localhost:11434)
OLLAMA_BASE_URL=http://localhost:11434

# Chat model for general conversation (gpt-oss:20b recommended)
OLLAMA_CHAT_MODEL=gpt-oss:20b

# MedGemma model for medical image analysis
OLLAMA_MEDGEMMA_MODEL=amsaravi/medgemma-4b-it:q8

# Fallback vision model (llava for general vision tasks)
OLLAMA_VISION_MODEL=llava:latest

# =============================================================================
# Qdrant Vector Store Configuration
# =============================================================================
# Use embedded mode (true) or connect to Qdrant server (false)
QDRANT_EMBEDDED=true

# Path for embedded Qdrant storage (used when QDRANT_EMBEDDED=true)
QDRANT_PATH=./qdrant_data

# Qdrant server settings (used when QDRANT_EMBEDDED=false)
QDRANT_HOST=localhost
QDRANT_PORT=6333

# Collection name for dermatology cases
QDRANT_COLLECTION=scin_dermatology

# =============================================================================
# HuggingFace (for embedding models)
# =============================================================================
# Optional: Token for private models
HUGGINGFACE_TOKEN=hf_...

# =============================================================================
# Database
# =============================================================================
DATABASE_URL=sqlite+aiosqlite:///./kiosk.db
# For production: postgresql+asyncpg://user:pass@localhost/kiosk

# =============================================================================
# SCIN Data
# =============================================================================
# Directory containing the Harvard SCIN dermatology dataset
SCIN_DATA_DIR=./scin_data

# =============================================================================
# Speech Configuration
# =============================================================================
# Whisper model size: tiny, base, small, medium, large
# Larger = better accuracy but slower and more memory
WHISPER_MODEL=base

# =============================================================================
# Server Configuration
# =============================================================================
HOST=0.0.0.0
PORT=8000
DEBUG=true

# =============================================================================
# Healthcare Facility Integration
# =============================================================================
FACILITY_API_URL=https://facility.example.com/api
FACILITY_API_KEY=...

# =============================================================================
# Supported Languages
# =============================================================================
# Comma-separated list of language codes
SUPPORTED_LANGUAGES=en,hi,ta,te,bn
